<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:PKHeX.Presentation.ViewModels;assembly=PKHeX.Presentation"
             mc:Ignorable="d" d:DesignWidth="560" d:DesignHeight="560"
             x:Class="PKHeX.Avalonia.Views.AutoLegalityModView"
             x:DataType="vm:AutoLegalityModViewModel"
             MinWidth="460" MinHeight="460" Width="560" Height="560">

    <DockPanel Margin="12">
        <TextBlock DockPanel.Dock="Top" Text="Auto Legality Mod" FontSize="16" FontWeight="SemiBold" />
        <TextBlock DockPanel.Dock="Top" Margin="0,2,0,8"
                   Text="Paste a Pokémon Showdown set. The engine builds a legal Pokémon for the loaded save."
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource ThemeTextMutedBrush}" FontSize="12" />

        <!-- Action bar -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Spacing="8" Margin="0,0,0,8">
            <Button Content="Paste from Clipboard" Command="{Binding PasteFromClipboardCommand}" />
            <Button Content="Legalize" Classes="accent" Command="{Binding LegalizeCommand}" />
            <Button Content="Load into Editor" Command="{Binding LoadIntoEditorCommand}"
                    ToolTip.Tip="Load the generated Pokémon into the main editor (current box slot / party)" />
            <ProgressBar IsIndeterminate="True" Width="120" VerticalAlignment="Center"
                         IsVisible="{Binding IsBusy}" />
        </StackPanel>

        <!-- Status -->
        <TextBlock DockPanel.Dock="Bottom" Text="{Binding StatusMessage}" TextWrapping="Wrap"
                   Margin="0,8,0,0" FontSize="12" />

        <!-- Input + legality report -->
        <Grid RowDefinitions="*,Auto,2*">
            <TextBox Grid.Row="0"
                     Text="{Binding ShowdownText}"
                     AcceptsReturn="True" AcceptsTab="False"
                     TextWrapping="NoWrap"
                     FontFamily="Consolas, Menlo, monospace"
                     Watermark="Paste a Showdown set here..."
                     VerticalContentAlignment="Top"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto" />

            <TextBlock Grid.Row="1" Text="Legality Report" FontWeight="SemiBold"
                       Margin="0,10,0,4"
                       IsVisible="{Binding LegalityReport, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />

            <Border Grid.Row="2"
                    BorderThickness="1" BorderBrush="{DynamicResource ThemeBorderBrush}"
                    IsVisible="{Binding LegalityReport, Converter={x:Static StringConverters.IsNotNullOrEmpty}}">
                <ScrollViewer>
                    <SelectableTextBlock Text="{Binding LegalityReport}" Padding="8"
                                         FontFamily="Consolas, Menlo, monospace" FontSize="12"
                                         TextWrapping="Wrap" />
                </ScrollViewer>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
